/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getMeMessage } from "../../funcs/getMeMessage.js";
import { GetMeMessageRequest$zodSchema } from "../../models/getmemessageop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetMeMessageRequest$zodSchema,
};

export const tool$getMeMessage: ToolDefinition<typeof args> = {
  name: "get-me-message",
  description: `Get a specific message`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getMeMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
